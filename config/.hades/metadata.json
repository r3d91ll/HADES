{
  "directory": {
    "path": "/config",
    "purpose": "Configuration files for all components",
    "domain": "configuration_management",
    "format": "YAML"
  },
  "contents": {
    "subdirectories": [
      {
        "name": "jina_v4",
        "purpose": "Jina v4 processor configuration",
        "key_settings": "model, vllm, chunking, keywords"
      },
      {
        "name": "isne",
        "purpose": "ISNE model and training configs",
        "key_settings": "architecture, training, enhancement"
      },
      {
        "name": "storage",
        "purpose": "Database configurations",
        "key_settings": "connection, collections, indexes"
      }
    ],
    "files": [
      {
        "name": "pathrag_config.yaml",
        "purpose": "PathRAG algorithm configuration"
      }
    ]
  },
  "context": {
    "configuration_hierarchy": [
      "Default values in code",
      "Config files (this directory)",
      "Environment variables",
      "Runtime parameters"
    ],
    "best_practices": [
      "Keep secrets in environment variables",
      "Document all settings",
      "Use meaningful defaults",
      "Support hot reloading where possible"
    ]
  }
}